# LDA 项目工作总结、优缺点与后续路线图

> 更新日期：2026-09-13  
> 评估分支：`codex/linux-server`  
> 评估提交：`322400c`  
> 本文定位：记录 2026-09-07 至 2026-09-12 的集中开发与实验工作，并给出当前项目可被证据支持的结论、主要风险和下一阶段计划。

## 0. 当前状态摘要

- 项目已从“可以运行的论文仿真代码”推进为具有物理记账断言、数值 oracle、配对实验、预注册停止规则和 Linux 后台运行能力的研究代码库。
- 当前共有 49 个受版本控制的 Python 文件，约 10,457 行 Python，其中测试约 1,607 行。
- 2026-09-12 在当前环境执行完整测试，`96/96` 项通过。
- 当前分支的研究提交和文档整理已同步到远端 `origin/codex/linux-server`；正式基线另以 `formal-baseline-20260913` 标签定位。
- 现有七组 8 种子 × 4096 帧正式 sweep 产生于最新 coupled 求解器和旧任务研究之前，不能直接代表当前代码版本。当前版本已通过测试、重新标定、smoke 及 64/512 帧 pilot，可以启动新的正式实验，但还没有重新完成同等级别的正式结果。
- `legacy` 仍是旧 BS 残余任务的默认处理策略。`budgeted-75%` 是有效的机制基线，但没有通过预注册的全部稳定性门槛；`joint_dpp` 及其 witness 版本已经按预注册结果终止，不应继续作为当前主路线追加网格点。
- 指标口径已经冻结：当前名为 PAoI 的 `Cost` 定义为“PAoI 导向的加权完成时延惩罚”。共享残量与 `w=2` 是有意放大跨帧延期代价的风险厌恶设计，不再把该量解释成接收端真实峰值 AoI，也不因此修改算法代码。

## 1. 这段时间完成的工作

### 1.1 工程清理与可复现性基线

1. 清理仿真源代码与版本管理边界：代码、测试和必要说明进入 Git；日志、结果、模型、论文材料和本地缓存由 `.gitignore` 隔离。
2. 将参数扫描输出改为时间戳目录，保存配置、提交号、环境、任务序列哈希、逐种子指标、日志和图表，避免不同批次相互覆盖。
3. 将环境随机性与策略随机性拆分；同一参数和种子的算法共享任务序列，并在比较前核对工作负载哈希。
4. 固定主要统计口径为统一后半程窗口，保留所有成功种子；同时保留 raw/cleaned 视图用于追溯，而不让算法各自选择有利窗口。
5. 增加同种子配对差值、Student-t 区间和稳定性诊断，避免把多个策略种子误当成独立环境重复。

### 1.2 Linux、GPU 与长任务运行支持

1. 增加无桌面 Linux 的 Matplotlib `Agg` 后端适配，移除对固定 Windows 盘符的依赖。
2. 多进程扫描显式使用 `spawn`，并限制 OMP/MKL/OpenBLAS/NumExpr 内部线程，减少 PyTorch `fork` 风险和嵌套线程争用。
3. DNN 支持 `cpu`、`cuda`、`cuda:N` 和自动设备选择；多 GPU 时将学习算法 worker 分配到可见 GPU，启发式算法不初始化 CUDA。
4. 增加 `trainctl`、`seedctl`、`ablationctl`、systemd service 和实时进度输出，使 SSH 断开后实验仍可运行，并能查看任务、worker 和帧进度。
5. 明确了性能边界：神经网络训练可用 GPU，但候选搜索和解析资源分配仍主要受 CPU 限制。

### 1.3 状态、队列与物理记账修正

1. 将每个 Actor 的输入扩展为完整的当前全局卫星竞争状态，替换滞后的卫星历史状态，并保留固定槽位外的汇总信息。
2. 修正 BS 与卫星积压的单一来源、跨帧残余任务、逐颗卫星能耗、卫星平均能耗分母及当前帧旧卫星服务记账。
3. 将 Lyapunov 漂移改为独立队列分解，并验证实际漂移、上界和队列守恒。
4. 环境运行时增加队列守恒和逐星能耗检查；测试覆盖随机队列更新、旧任务债务延续及新旧能耗分项一致性。
5. 修正高负载下优化器对偶区间和退化分支，增加零时间、零任务、非活跃能量约束等边界测试。

### 1.4 资源分配目标统一与加速

1. 识别并复现了原下层逐任务目标与上层共享残余时延评分不一致的问题，以及空队列时错误返回零频率的确定性反例。
2. 新增 coupled BS/LEO 求解路径，使下层分配与上层共享残余任务目标一致；保留旧求解器可行解作为 warm start。
3. 小节点使用完成集合枚举，大节点使用有限集合近似；加入目标下界剪枝、批量旧求解器 warm start 和重复联合动作缓存。
4. 在隔离 CPU 基准上，默认 `J=10、delta_t=0.5` 从约 6.0 秒/帧降到约 1.6 秒/帧。该数值是工程基准，不代表所有机器或场景。
5. 用稠密网格 oracle、标量/向量/批量实现交叉验证新求解器。诊断中固定 MTD 动作在三个场景均降低或保持 PAoI 代理，并显著降低队列，支持修复方向正确。

### 1.5 学习、候选与消融实验完善

1. 修正 Actor 输入维度、归一化和掩码监督；本地执行用户无意义的 `b` 位不再参与损失。
2. 网络改为 LayerNorm 加两个残差块；默认隐藏宽度随扩展状态增至 640。
3. 候选组合由错误的同下标截断改为受限坐标搜索，避免某个全本地 BS 截断其他 BS 的候选。
4. 可选加入 COB/MTD 动作作为同状态候选保护；实验表明它只在约 0%–1.95% 的后半程帧改变动作，没有带来稳定的跨帧收益。因此“候选没有覆盖基线”不是当前主要矛盾。
5. 将 LDA2 明确为同时从上层评分和 BS/LEO 下层分配中去除 PAoI 的消融，而不是 Actor-Critic。消融结果显示仅删上层、仅删下层和同时删除都会明显恶化时延或队列。
6. 2026-09-13 已在当前 `coupled + legacy` 主实现上重新标定 `Q_ref、PAoI_ref、E_ref`，对能量项只统计活跃非零帧，并使用对数阻尼避免尺度反馈振荡；记录见 [`docs/CALIBRATION_20260913.md`](docs/CALIBRATION_20260913.md)。

### 1.6 旧 BS 残余任务研究

1. 建立旧任务跨帧反馈理论说明，区分“固定总频率下最小化最晚完成时间”和“长期 PAoI/能耗最优”，证明二者不能混为一谈。
2. 实现 `budgeted` 策略、负载泛化脚本、分层种子设计、逐 BS 轨迹审计和可恢复的阶段流水线。
3. 阶段 A 的 48 次运行完成后，25%/50%/75% 分别通过 8/12、10/12、12/12，按冻结规则选 75% 进入新环境确认。
4. 阶段 B 的 60/60 次 2048 帧运行完整。75% 在全部 15 个“负载 × 环境”比较中相对 legacy 同时降低 PAoI 代理、队列和 BS 能耗；但严格稳定性判据只通过 22/30，未达到预注册的 30/30，因此流水线正确停止，没有启动 4096 帧确认。
5. 失败的 8 次均来自物理队列斜率略大于零，而能耗与能量队列判据为 30/30 通过。这说明固定份额可有效控制能量反馈，但不能被表述为已验证的通用稳定策略。

### 1.7 Joint DPP 路线与负结果

1. 实现 `joint_dpp` 原型，让旧任务聚合频率与新任务频率通过同一个虚拟能量队列价格竞争，并加入边界、驻点和时间转折点候选。
2. C0 的正确性和缓存后性能门槛通过；C1 的 36/36 次运行完整，联合频率确实动态变化。
3. C1 中 joint 在低、中负载优于 legacy，但三个负载均劣于 75% 机制基线，且 L=12 的平均运行时间约为 75% 的 7.12 倍，因此没有进入昂贵的 C2。
4. 按预注册方案加入冻结的 75% witness 后，W1 的 36/36 次新种子运行完成；joint-w75 在 6/6 个“负载 × 环境”配对中同时恶化 PAoI 代理、队列和 BS 能耗。
5. 已按停止规则终止当前联合旧频率搜索路线，不再增加 witness、网格点或事后容差。这个负结果有价值：单步目标支配并不能推出改变动作和状态轨迹后的长期指标支配。

## 2. 项目优点

### 2.1 研究过程开始具备可证伪性

项目最明显的进步不是某个指标变好，而是能够主动构造反例、冻结实验设计、保存负结果，并在门槛失败时停止。阶段 B、C1 和 W1 都没有通过改容差或挑种子包装成正结果，这显著提高了论文结论的可信度。

### 2.2 物理记账和数值正确性基础较强

队列守恒、逐星能耗、跨帧债务、Lyapunov 漂移和不同优化器实现之间都有自动验证。小规模问题还使用独立稠密网格 oracle，而不是只验证“代码能够运行”。96 项测试覆盖了大量过去容易悄悄出错的边界。

### 2.3 实验可复现性优于一般本科论文代码

配置、源码提交、环境、随机种子、任务哈希、原始轨迹、汇总指标和源码快照可以形成完整证据链。同环境内嵌套策略种子、环境级聚合和配对统计也避免了常见的伪重复问题。

### 2.4 模块边界基本清楚

环境、信道、Agent、BS/LEO/UAV 优化器、目标函数、绘图和实验入口已经分开。标量、向量和批量优化器虽然看起来重复，但承担数值互证作用，不应简单删除。

### 2.5 具备服务器长实验的实际运行能力

无头绘图、spawn、多 GPU 设备分配、线程限制、systemd 后台服务、状态查看和中断恢复已经覆盖了长实验最常见的运维问题。项目不再依赖本地 IDE 窗口保持开启。

### 2.6 对算法边界的表述较诚实

README 与指标定义文档已明确区分加权完成时延惩罚与真实 PAoI、有限候选近似与全局最优、短程机制筛查与正式长实验、单步目标保护与长期轨迹效果。这些限定应继续保留到论文正文。

## 3. 项目缺点与风险

### 3.1 核心论文指标的解释边界

当前 `Cost` 不是从信息生成、送达和接收端年龄演化中直接计算的真实峰值 AoI。
项目已经选择表述修正路线：将研究目标统一定义为 PAoI 导向的加权完成时延
惩罚，并明确共享残量与 `w=2` 用于放大延期风险。剩余风险是论文、图注和脚本
输出仍可能沿用历史 `PAoI` 字段名，发布前必须按
[`docs/METRIC_DEFINITION.md`](docs/METRIC_DEFINITION.md) 统一人工可见标签。

### 3.2 最新代码缺少同等级正式实验

已有七组完整 sweep 使用的是较早提交；之后状态、物理记账、coupled 求解器和旧任务机制都有实质变化。现在的短程诊断能够排错和筛路线，却不能替代当前版本的 8 种子 × 4096 帧正式结论。

### 3.3 当前学习器的理论定位需要更准确

Actor 通过搜索得到的优胜动作进行监督学习，本质更接近“学习加速组合决策/模仿在线搜索”，而不是标准的 Actor-Critic 或端到端强化学习。标签又来自有限候选搜索，因此低训练损失只说明网络能拟合当前搜索结果，不证明策略接近全局最优。

### 3.4 状态和动作搜索仍是有界近似

固定 `S` 个逐星槽位之外的信息被汇总，尚无严格的马尔可夫充分性证明；跨 BS 候选采用有限轮坐标搜索；大节点 coupled 求解使用有限完成集合。这些选择在工程上合理，但论文不能据此声称完整状态、全局联合搜索或连续域全局最优。

### 3.5 跨帧旧任务仍没有合格的默认新方案

legacy 可能形成“新任务延后—下一帧满频清算—挤压新任务”的反馈；75% 虽改善连续指标，却未通过全部稳定性门槛；joint 路线又被新种子实验否定。现在应承认该问题仍开放，而不是从三个候选中强行选一个包装为最终算法。

### 3.6 正式实验成本较高

候选搜索和解析分配主要在 CPU 上，joint 路线可达到 75% 基线的 3.47–7.21 倍运行时间。继续堆叠频率网格的边际价值很低，也会挤占真正需要的指标闭环和正式复现实验时间。

### 3.7 工程入口和文档逐渐分散

项目已有多个实验入口、汇总脚本、控制脚本、阶段报告和 systemd 服务。它们记录了研究过程，但新使用者难以判断哪个是当前推荐入口、哪些只用于历史复现。全局 `config.py` 也使不同实验的冻结配置较依赖脚本内约定。

### 3.8 长任务恢复粒度仍偏粗

当前可以复用已完成运行，但单个未完成实验通常需要从头开始；模型没有 checkpoint、优化器状态和回放池的统一断点续训接口。对 4096/8192 帧训练来说，这会浪费计算资源。

### 3.9 依赖和持续集成仍可加强

除 PyTorch 外，NumPy/SciPy/Matplotlib 只设下界，未来版本可能改变数值行为；仓库目前没有自动 CI 证据。测试时还出现 Matplotlib 配置目录不可写的告警，虽然自动回退 `/tmp` 且不影响本次通过，但服务器脚本应显式设置可写的 `MPLCONFIGDIR`。

### 3.10 本地结果需要归档策略

当前 `logs/` 与 `results/` 约占 672 MiB，包含正式 sweep、预注册实验、原始轨迹和源码快照。它们是研究证据，不应作为“垃圾文件”直接删除；但也不应只保存在单台机器。项目缺少明确的冷归档、校验和、保留级别和恢复说明。

### 3.11 结果归档仍只在本机

源码侧的备份缺口已经关闭：`codex/linux-server` 的 HEAD 与
`origin/codex/linux-server` 一致，并已由只读标签 `formal-baseline-20260913`
定位。剩余缺口在数据侧：`logs/`（约 400 MiB）与 `results/`（约 275 MiB）只存在
于这一台机器，没有冷归档、校验和或恢复说明。这部分包含正式 sweep、阶段 B、C1、
W1 的原始轨迹和源码快照，是论文证据链的一部分，不应只保留单副本。

## 4. 对当前结果的合理表述

可以说：

- 已修复多项状态、队列、能耗和资源分配一致性问题，并由自动测试与小规模数值 oracle 支持。
- 当前 LDA 在部分场景优于 COB/MTD，但现有证据不支持“所有参数下全面优于基线”。
- coupled 求解器对固定动作有明确收益，但学习策略收益会受跨帧轨迹和随机性影响。
- 75% 固定份额是有效的机制基线，但不是已确认稳定的正式默认策略。
- 当前 joint DPP 有完整负结果，说明单步目标改进没有转化为更好的长期轨迹。

暂时不能说：

- 当前代码已经复现论文全部曲线；
- 当前 Cost 已严格等于真实 PAoI；
- LDA、coupled 或 joint DPP 达到全局最优；
- 512/2048 帧筛查证明了渐近队列稳定或长期能量约束；
- 新旧任务联合调度问题已经解决。

## 5. 未来可行计划

### P0：先保护现有成果（半天内）

1. ~~将当前 6 个未推送提交推送到远端同名分支，并打一个只读标签。~~
   已完成：HEAD = `origin/codex/linux-server` = 标签 `formal-baseline-20260913`。
2. 为关键结果目录生成清单和 SHA-256 校验和；至少保留“正式 sweep、阶段 B、C1、W1”四类，其余开发探针按保留策略归档。
3. 把当前 Conda 环境导出为锁定文件，记录 CUDA、驱动、CPU 和线程环境；避免仅依赖宽松的 `requirements.txt`。
4. 冻结当前结论：停止给 joint 路线追加 witness 或网格点。

完成门槛：源码和关键结果至少有两个独立副本，远端能够定位到 `322400c`，关键产物校验和可复核。

### P1：统一时延惩罚表述（已确定）

1. 不改动现有目标函数和历史字段名；
2. 将论文目标定义为 PAoI 导向的加权完成时延惩罚；
3. 明确 `w=2` 和共享残量是有意增加延期惩罚，而不是 PAoI 恒等式；
4. 图、表和正文统一使用“时延惩罚”，不再声称测量了真实峰值 AoI。

完成门槛：指标定义文档、README、实验指南和论文正文使用同一公式与名称。若未来
另行研究真实 AoI，应作为新增指标单独设计，而不追溯性改变本轮正式实验定义。

### P2：选择最小可交付研究主线（2–4 天）

建议优先采用“稳妥论文路线”：

1. 以 coupled + legacy 作为可复现主实现，明确 legacy 的局限。
2. 将 budgeted-75% 作为机制消融，将 joint DPP 作为负结果或附录，不宣称解决旧任务调度。
3. 下一代跨帧策略如继续研究，应以当前时延惩罚定义或另行新增的真实 AoI 指标明确预注册。

如果时间允许再开“研究扩展路线”：重新定义包含旧任务年龄、物理积压和能量债务的跨帧状态与目标，优先考虑小规模滚动时域/MPC oracle 或显式双层策略作为教师，而不是继续扩展当前单帧频率候选网格。新路线必须使用新种子和新的预注册文件。

### P3：重跑当前版本的正式实验（约 1–2 周计算时间）

1. 在已冻结的时延惩罚定义和默认策略下重新标定参考尺度。
2. 先运行 smoke、64/512 帧 pilot，验证哈希、设备分配、内存、吞吐和指标有限性。
3. 再运行核心实验，而不是一开始无差别重跑全部图：默认点、一个低负载点、一个高负载点、一个用户规模点。
4. 每点至少 8 个独立环境种子，建议 4096 帧起步；如果后半程稳定性仍不足，再延长到 8192 帧，而不是删除“不收敛”的成功种子。
5. 同时报告加权完成时延惩罚、物理队列、逐 BS 能耗、能量虚拟队列、训练更新数和运行时间；不将该时延惩罚改称真实 PAoI。
6. 主要结论使用同环境配对差；多参数结论增加多重比较说明或控制。

完成门槛：当前提交、冻结配置、全部种子和统计脚本能够从零复现主表；结论不依赖 cleaned 视图。

### P4：降低后续维护成本（可与实验并行）

1. 推荐入口已初步收敛为 `scripts/ldactl`，覆盖 test、oracle、smoke、calibrate、sweep 和 formal 控制；历史入口保留兼容层。
2. 将全局配置迁移为 dataclass 加 YAML/JSON 冻结配置，运行时保存完全展开后的参数。
3. 增加按固定帧间隔保存模型、优化器、回放池、环境 RNG 和进度的 checkpoint。
4. 增加 CI：CPU 上运行 96 项单元测试、CLI smoke 和关键小型 oracle；长实验仍留在服务器。
5. 固定一份可复现依赖锁，并在服务器脚本中设置 `MPLCONFIGDIR`。
6. 建立 `results/INDEX.md` 或外部归档清单，标记 `keep`、`archive`、`discardable` 三种级别。

### P5：论文与答辩材料同步（正式实验后）

1. 按实际代码统一符号：逐用户/逐 BS 队列、逐星能耗、跨帧旧任务和代理目标。
2. 主文只保留能被当前版本正式实验支持的结论；负结果、停止规则和工程诊断放入附录。
3. 每张图注明提交号、参数文件、种子集合、统计窗口、误差条含义和指标单位。
4. 单独列出威胁有效性的因素：代理指标、有限候选、状态截断、有限时域和服务器数值环境。

## 6. 建议的文件保留与清理策略

### 必须保留

- `core/`、`utils/`、`tests/`、`config.py` 与主要运行入口；
- `analysis/` 中的预注册、最终结果报告、汇总脚本和流水线；
- `docs/old_bs_cross_frame_theory.md` 等理论说明；
- 阶段 B、C1、W1 与正式 sweep 的原始 JSON/NPZ、manifest、日志和源码快照；
- systemd 与控制脚本，直到服务器实验完全结束并完成迁移。

### 可以随时删除并重新生成

- `__pycache__/`、`*.pyc`、`*.pyo`；
- `.pytest_cache/`、coverage 和临时 Matplotlib 缓存；
- `analysis/test_results.txt`、`analysis/coupled_oracle_checks.json` 等本地验证输出；
- 明确失败且已被最终报告完整替代的 smoke 图和重复渲染文件，但删除前应先对照结果索引。

### 需要先归档再删除

- `logs/`、`results/` 中的长实验；
- 任何包含唯一随机轨迹、训练日志或源码快照的目录；
- 阶段性失败实验：它们可能是论文负结果和审计链的一部分。

## 7. 本次整理实际清理内容

本次只删除了可自动重建且已被 `.gitignore` 排除的文件：

- 43 个 Python 字节码文件及空的 `__pycache__` 目录；
- `analysis/test_results.txt`；
- `analysis/coupled_oracle_checks.json`。

合计删除 45 个文件，释放空间约 680 KiB。没有删除 `logs/`、`results/`、源码、测试、预注册文档、论文材料或实验原始数据。

## 8. 下一次接手时的最短检查清单

```bash
git status --short
git log --oneline -n 8
python -X utf8 -m unittest discover -s tests -p 'test_*.py' -v
python analysis/run_old_bs_pipeline.py \
  --control-dir results/old_bs_pipeline/phase_b_current \
  --evaluate-only
```

随后先确认三件事：当前提交是否已推送、时延惩罚定义及参考尺度是否已经冻结、正式实验是否明确记录了代码提交与配置。三者没有完成前，不建议启动新一轮大规模 sweep。
